import Header from "./components/Header/Header";

function App() {
  return (
    <div>
      <Header />

      {/*Add Form here */}

      {/* Todo: Show below table conditionally (only once result data is available) */}
      {/* Show fallback text if no data is available */}
      {/* Add in Result element here */}
    </div>
  );
}

export default App;
